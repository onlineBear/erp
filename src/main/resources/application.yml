spring:
  profiles:
    active: dev     # 选用一个环境
  datasource:
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  http:
    encoding:
      force: true
      enabled: true
      charset: UTF-8
  mvc:
    throw-exception-if-no-handler-found: true       # 出现错误时, 直接抛出异常
  resources:
    add-mappings: false                             # 不要为资源文件建立映射
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8          # 东八区
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

  #mybatis:
  # type-aliases-package: org.anson.demo.mapper
  #mapper-locations: classpath:mapper/**/*.xml   # 当 mapper 和 xml 不在同一个包下, 需要制定 xml的路径

logging:
  config: classpath:spring-logback.xml

server:
  tomcat:
    uri-encoding: UTF-8
  port: 8082  # 本地端口

jwt:
  issuer: anson    # token 的发行者
  subject: minProject    # token 的题目
  audience: user  # token 的客户

mybatis-plus:
  global-config:
    db-config:
      id-type: id_worker_str    # 全局默认主键类型 雪花算法-字符串
      table-underline: false
  configuration:
    map-underscore-to-camel-case: false     # 是否开启自动驼峰命名规则（camel case）映射
    call-setters-on-nulls: true

comstom:
  page:
    defaultPageSize: 50     # 默认分页大小
  encrypt:
    key: QBnro6SPlhL5gKLPHglf # 加密的key
    salt: pryF8Lz5fi          # 加密盐值
  export:
    maxTotal: 500000          # 最大导出数量
