<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zmqy.erp.mapper.mis.biz.sys.security.UserMenuMapper">

    <select id="geRecentUserMenu" resultType="map">
        select distinct ur.userId, rm.menuId
        from tRoleMenu as rm
                 join tRoleUser as ur on rm.roleId=ur.roleId
        order by ur.userId, rm.menuId
    </select>

    <insert id="batchInsert" parameterType="org.zmqy.erp.model.mis.entity.sys.security.UserMenu">
        insert into tUserMenu
            (id, userId, menuId, createUserId,
             createTime, lastUpdateTime)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.userId}, #{item.menuId}, #{item.createUserId},
            #{item.createTime}, #{item.lastUpdateTime})
        </foreach>
    </insert>
</mapper>
