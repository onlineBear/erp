<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.anson.miniProject.core.mapper.sys.MenuMapper">
    <update id="updateChildByParent">
        update menu
        set clientDictId = parent.clientDictId,
            path = parent.path + '/' + children.id,
            lastUpdateTime = #{operTime}
        from menu as parent
        join menu as children on parent.path like (children.path + '%')
        where parent.id in
        <foreach collection="parentMenuIdList" item="parentMenuId" open="(" close=")" separator=",">
            #{parentMenuId}
        </foreach>
    </update>
</mapper>